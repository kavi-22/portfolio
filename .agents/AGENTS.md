# Workspace Rules

## Git Workflow
- Always work on the `develop` branch for any feature, fix, or content update.
- Do not commit directly to `main`.
- Push changes to `origin develop` and provide the Pull Request (PR) link to merge `develop` into `main`.
